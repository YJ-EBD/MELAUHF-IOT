-- 00_create_database.sql
-- MySQL/MariaDB database creation for for_rnd_web

-- NOTE: 이 파일은 참고용입니다.
-- 실제 운영에서는 root(또는 DBA 권한)로 실행하세요.

CREATE DATABASE IF NOT EXISTS `ABBAS`
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

-- USE `ABBAS`;
